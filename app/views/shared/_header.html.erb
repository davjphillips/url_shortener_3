<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
					<% if user_signed_in? %>
          <%= link_to "asko.me", links_path, :class => "brand"%>
					<% else %>
					<%= link_to "asko.me", new_link_path, :class => "brand"%>
					<% end %>
          <div class="nav-collapse">
					<ul class = "nav pull-left">
						<% if user_signed_in? %>
						<li><%= link_to "New Link", new_link_path %></li>
						<li><%= link_to "My Links",links_path %></li>
						<% end %>
					</ul>	
            <ul class="nav pull-right">
							<% if user_signed_in? %>
							<li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
							<% else %>
						
							<li><%= link_to "Sign In", new_user_session_path,:class => "pull-right" %></li>
							<% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
</div>
